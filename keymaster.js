// Placeholder for the keyboard-shortcut dependency the client's index expects
// (the reference client ships keymaster2.js here). No-op stub so the <script> tag resolves;
// replace with a real keyboard lib if the client requires window.key bindings.
window.key = window.key || function () {};
