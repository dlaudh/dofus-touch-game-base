{
  "_comment": [
    "Regex patches applied to the downloaded client bundle (build/script.js)",
    "before it runs. Keyed to the minified client build (currently v3.2.13);",
    "patterns are JavaScript RegExp; non-matching ones are skipped with a",
    "warning. Each entry: { file, find, replace, note }."
  ],
  "patches": [
    {
      "file": "build/script.js",
      "find": "return t\\.m=e,t\\.c=i,t\\.p=\"\",t\\(0\\)",
      "replace": "window.singletons=t;return t.m=e,t.c=i,t.p=\"\",t(0)",
      "note": "Expose the webpack module registry as window.singletons. Harmless; enables later hooks."
    },
    {
      "file": "build/script.js",
      "find": "toggleClassName=function\\(e,t\\)\\{return ",
      "replace": "toggleClassName=function(e,t){if(!this.rootElement)return;return ",
      "note": "Guard toggleClassName against a null rootElement. On desktop there is no phone notch, so displayNotch()->toggleClassName() runs on a UI wrapper whose rootElement is null and throws during resize. Also protects any other null-rootElement toggle."
    },
    {
      "file": "build/script.js",
      "find": "(loginWithWebAuth=function\\(e,i\\)\\{return \\w{1,2}\\.setHaapiKey\\(e\\.accessKey,e\\.refreshKey)\\)",
      "replace": "$1,{save:!0})",
      "note": "Persist the Haapi key to localStorage (save:true) so the session survives restarts. loginWithWebAuth calls setHaapiKey without the save flag, so HAAPI_KEY/HAAPI_REFRESH_TOKEN are never written and the login is lost on relaunch."
    },
    {
      "file": "build/script.js",
      "find": "(\\w+)\\.viewportWidth=\\1\\.viewportWidth\\|\\|(\\w+)\\.document\\.documentElement\\.clientWidth,\\1\\.viewportHeight=\\1\\.viewportHeight\\|\\|\\2\\.document\\.documentElement\\.clientHeight",
      "replace": "$1.viewportWidth=$2.document.documentElement.clientWidth,$1.viewportHeight=$2.document.documentElement.clientHeight",
      "note": "Make window resize actually resize the game. updateScreen() caches viewportWidth/Height (viewportWidth=viewportWidth||clientWidth) because the client assumes a device screen never changes size (isDevice=isIOS||isAndroid, which we spoof). Removing the cache makes _resizeUi() (which we drive on window resize in fixes.js) recompute from the current clientWidth/clientHeight."
    },
    {
      "file": "build/script.js",
      "find": "isPortraitMode=function\\(\\)\\{[^}]*\\}",
      "replace": "isPortraitMode=function(){return !1}",
      "note": "Force landscape layout so the UI docks on the right instead of stacking at the bottom. The client picks portrait (bottom UI) when innerHeight>innerWidth; on desktop we always want the landscape (right-side) layout."
    }
  ]
}
