// Runtime fixes applied AFTER the client bundle loads (see index.html initDofus).
// Clean placeholder — desktop-specific runtime shims go here as they are found
// during live testing against the real client (e.g. keyboard, focus, resize).
// Intentionally minimal: no gameplay modifications.
(function () {
  "use strict";
  // console.log("[dtd] fixes.js loaded");
})();
